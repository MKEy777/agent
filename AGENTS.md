# AGENTS — 多 Agent 架构对比分析

## 项目定位

本项目对四个开源 Agent 项目进行系统性横向对比：

| 项目 | 定位 | 语言 | 组织 |
|------|------|------|------|
| **deer-flow** | ByteDance 超 Agent 框架 | Python + Next.js | 企业级 LangGraph 编排 |
| **hermes-agent** | Nous Research 自进化 AI Agent | Python + TypeScript | 个人全能助手 |
| **openclaw** | OpenClaw Foundation 个人 AI | Python + TypeScript | 双实现：Python 版 AI Gateway + TS 版个人助理 |
| **claudecode** | Claude Code Python 重实现 | Python | Agent 内核实现 |

目标不是罗列代码，而是**提炼出可对比的架构模式**（每个模块怎么设计、为什么这么设计、优缺点），最终产出可读性强的对比文档。

---

## 工作流程

第一步（全局理解）全程只做一次，后续步骤按**单个模块逐一循环**——写完了模块 1 的文档，再开始模块 2，以此类推，不一口气写 12 个。

### 第一步：全局理解

先阅读每个项目的顶层结构和 README，建立全景认知。每个项目已有简要概况（见上方表格），但必须读原文确认。

### 第二步：确定模块范围

从下方模块清单中选定一个模块，然后针对它展开后续研究。

### 第三步：源码研究（子代理并行）

选定一个模块后，根据文档类型选择研究方式：

**横向对比文档**（`docs/comparisons/`）：用子代理并行读四个项目。每个子代理负责一个项目，读取 AGENTS.md 找到关键文件，然后读核心实现 + 周边文件，返回该项目的写作计划。四个项目同时读，而不是逐个看完再读下一个。注意 openclaw 有 Python 和 TS 两种实现，openclaw-python 的 AGENTS.md 和 openclaw-2026.7.1 的 AGENTS.md 都需要读，子代理提示词中明确要求同时覆盖两份 AGENTS.md。

**项目深入文档**（`docs/{project}/`）：只读一个项目，但要用子代理把该项目该模块的周边文件也覆盖到。例如研究 deer-flow 的 Agent 主循环，子代理分别读 harness 层、中间件链、TUI 层，确保不遗漏相邻子系统的依赖。

子代理提示词必须包含模块名称、要研究的具体内容点、以及写作计划的输出格式。

```
# 子代理提示词模板
研究 [项目名] 的 [模块名] 模块源码。

1. 读项目根目录的 AGENTS.md 找到关键文件位置
2. 读核心实现文件，理解主流程
3. 读周边文件（API 层、数据模型、上下游依赖）
4. 按以下格式输出写作计划：

## 核心文件清单
## 架构总览
## 主流程 trace
## 核心设计选择（2-3 个）
## 工程优化
## 边界情况
## 与其他模块的接口
```

你收到所有子代理返回的写作计划后，先补充你自己对其中一个项目的研究（如果子代理数量不够覆盖 4 个项目），然后汇总进入产出阶段。

**注意：** 子代理只做读代码 + 输出写作计划，不做正式文档的写作。写作计划给你看，不是给最终读者看。

**完整性自检：** 确认每个项目的写作计划覆盖了以下五类文件（缺一不可）：
- 核心实现文件（该模块的主入口）
- API/适配层（模块与外部系统的边界）
- 数据模型/事件类型（模块的通信协议）
- 相邻子系统文件（模块的上下游依赖）
- 同一功能的备选实现（如流式执行 vs 批次执行，便于对比分析）

### 第四步：写写作计划（给自己看）

在研究完源码后、动手写正式文档之前，先输出一份**写作计划**。这份计划不是给人看的，而是你在阅读源码后「组织一次思考」的产出物——它确保你在写正式文档时不缺要点，也让"读完了"这个状态可验证。

写作计划直接在源码阅读的过程中同步记录，结构如下：

```
# [项目名] - [模块名] - 源码阅读笔记

## 核心文件清单（已读）
列出读过的所有文件及路径，注明每个文件的核心职责

## 架构总览（一句话）
该模块在整个项目中的角色

## 主流程 trace
从输入到输出的完整调用链，标注关键函数名

## 核心设计选择（2-3 个）
每个设计选择：是什么 → 为什么选它 → 代价是什么

## 工程优化
错误恢复、并发控制、防御性编程等

## 边界情况
作者特别处理了哪些极端情况

## 与其他模块的接口
输入来源、输出去向、依赖方向
```

这份笔记在产出正式文档后可以保留，也可以丢弃。它的价值在于强迫你在阅读完成后**组织一次思考**，避免读完了就忘——读完了七个文件不代表读懂了，能用自己的话写清楚每个设计选择的来龙去脉才是真的读懂了。

## 模块清单参考

以下 12 个模块按"智能体运行时栈"自底向上排列（按编号对应 `docs/` 下的文件）：

按"智能体运行时栈"自底向上分层：

```
消息通道 ─── 可观测性 ─── 安全与沙箱  ← 跨层关注点
  插件/扩展系统
  技能系统 (Skills)
  子代理/编排
  记忆系统 (Memory)
  MCP 协议集成  ─── 工具系统 (Tools)  ← 并列，MCP 是工具的标准化协议
  Agent 主循环
  LLM 接入层                          ← 最底层抽象
```

#### 核心模块清单

1. **LLM 接入层** — Provider 抽象、模型选择与路由、prompt caching、fallback 策略、结构化输出
2. **Agent 主循环** — 消息处理管道、意图路由、工具调用调度、状态管理
3. **工具系统** — 工具定义、注册、发现、执行生命周期
4. **MCP 集成** — 客户端实现、服务器管理、传输协议、与原生工具系统的关系
5. **技能系统** — SKILL.md 规范、激活机制、权限、热加载
6. **记忆系统** — 持久化策略、检索方式、会话管理、向量 vs 结构化
7. **子代理/编排** — 任务委派、并行执行、结果合并、错误恢复
8. **插件/扩展系统** — 插件的定义、加载、作用域、生命周期钩子
9. **消息通道/IM 集成** — 多平台支持、消息路由、认证、webhook
10. **安全与沙箱** — 代码执行沙箱、网络策略、权限模型、密钥管理
11. **配置与环境管理** — 配置层级、profile 支持、多实例
12. **可观测性** — 日志、追踪、token 用量追踪、成本监控、调试支持

### 第五步：产出文档

`docs/` 下有两类文档，每类 12 个模块（对应核心模块清单 1-12），所有文档都必须遵循相同的教学型写作方法论（见下文）：

| 类型 | 位置 | 目的 | 聚焦 |
|------|------|------|------|
| 横向对比 | `docs/comparisons/` | 四个项目同一模块的横向对比 | 差什么，不同项目为什么不同 |
| 项目深入 | `docs/{project}/` | 单个项目某个模块的实现深入 | 一个方案具体怎么设计、为什么这么设计 |

**通用写作规则：**
- **面向读者**：面试准备 + 学习，目标是理解架构设计思想，不是学会调用 API
- **不要堆代码和函数名**：关键函数名可以出现（如 `query_loop()`），但不要逐行解释代码。读者关心的是「为什么这么设计」，不是「怎么实现的」
- **聚焦核心机制**：每个模块抓每个项目最与众不同的 2-3 个设计选择展开，不要面面俱到
- **核心机制必须配详细流程解析**：对最重要的那个机制不能只写"用了 X 模式"，必须给出 step-by-step 的执行流（用 mermaid 的 sequenceDiagram 展示时序，或用 stateDiagram 展示状态流转，或用 graph 展示数据路径），让读者能在大脑中模拟一次完整的执行过程
- **工程优化单独列出**：在核心设计思想之外，把工程落地的关键优化（错误恢复策略、并发控制、资源管理、性能优化、防御性编程）单独标注出来。面试官常追问"这个设计在工程上有什么坑？你们怎么解决的"——这些就是答案
- **用 mermaid 图示**：遇到复杂机制（如状态机流转、中间件链顺序、消息路由拓扑），优先用 mermaid 并配上凝练的解释
  ````mermaid
  graph LR
      A[用户输入] --> B{意图路由}
      B --> C[工具A]
      B --> D[工具B]
  ````
- **诚实标注权衡**：每个架构选择一定牺牲了某些东西。明确说出来（这个方案损失了什么？）
- **保持可读性**：用白话、对比表格。避免学术论文式的抽象描述，不要用比喻
- **段落优先**：多用整段的话组织信息，避免大量分点罗列把篇幅拉长。相似的信息合并到一段里说，提高信息密度
- **语言要求**：除了必要的英文术语（函数名、框架名、协议名），全部使用中文。文档要面向能在面试中用中文讲清楚的场景

**教学型写作方法论：**

所有文档的本质都是教学材料。每篇文档应当引导读者经历从"看到问题"到"理解设计选择"的完整认知过程。借鉴 Socratic 教学法的核心原则：

#### 核心原则

| 原则 | 含义 | 在文档中的体现 |
|------|------|---------------|
| **先问后答** | 在给出答案前先提出问题，激活读者的思考 | 每个小节以问题开头，再展开分析 |
| **预测-验证** | 让读者先预测某个方案会怎么做，再揭示实际设计 | 先设问"如果你来设计这个模块，你会怎么做？"，再展示各项目的真实选择 |
| **追设计动机** | 不只说"是什么"，追问"为什么是它而不是别的" | 每个方案必须附带设计动机和当时面临的核心 trade-off |
| **对比引导** | 不同项目面对同一问题给出了不同答案——差异本身就是教学内容 | 始终围绕同一问题展开四个项目的回答，突出岔路口 |
| **主动回忆** | 在文档末尾设置检查点，让读者自己复述、比较、评价 | 每篇文档末尾设"面试要点"板块 |
| **渐进难度** | 从直觉层问题开始，逐步深入到细节权衡 | 每节内部结构：直觉问题 → 方案展示 → 权衡分析 → 面试要点 |

#### 对比文档统一格式

每篇对比文档（`docs/comparisons/` 下）必须遵循以下结构：

```
# 模块名称

## 读前思考（1-2 个问题，激发好奇心）
- [问题1] 引出该模块的核心矛盾
- [问题2] 让读者预测某个项目可能的方案

## 核心问题
一句话定义这个模块解决什么问题。用对比表格列出四个项目的定位差异。

## 方案展示（每个项目 1-2 段 + mermaid 图）
每个项目的方案都围绕同一组维度展开，方便横向对比。
结构统一为：设计选择 → 为什么选它 → 牺牲了什么

## 横向对比
核心部分。用表格 + mermaid 呈现差异，聚焦"岔路口"——为什么面对同一问题，不同项目走了不同路。

## 面试要点
2-3 个深度追问，每个问题要呈现正反两方面的考量（"为什么选 A 而不是 B"），参考答案给出场景依赖的判断标准而非绝对答案。问题可涵盖：
- "如果去掉某个约束，方案会怎么变？"
- "这个设计的代价在哪？"
- 跨项目方案在另一个场景下的适配性
- "核心机制是怎么一步步执行的？边界情况怎么处理？"

注意：问题要聚焦设计决策和 trade-off，不要钻到实现细节里。问"怎么处理 429 错误？"而非"SSE 解析失败时为什么用空 dict 降级？"——前者考察对错误恢复策略的理解，后者只是记忆题。
```
#### 项目深入文档统一格式

每篇项目深入文档（`docs/{project}/` 下）必须遵循以下结构：

```
# 模块名称

## 读前思考（1-2 个问题，激发好奇心）
- [问题1] 引出该模块的核心矛盾
- [问题2] 引导读者预测该项目的方案

## 核心问题
一句话定义这个模块解决什么问题。说明该项目在这个问题上的定位差异。

## 方案展示（3-5 个设计选择 + mermaid 图）
每个设计选择都按统一结构展开：
  设计选择 → 为什么这么选 → 牺牲了什么

围绕该模块最与众不同的 2-3 个设计选择，不要面面俱到。
用 mermaid 呈现架构总览、状态机、或调用链。

**必须包含本模块核心机制的完整执行流 trace：** 选一个具体输入（如一次用户操作、一次工具调用、一次连接建立），用 sequenceDiagram 展示从输入到输出的全链路——经过哪些组件、每步做什么、数据怎么流转、异常走哪条分支。不能只画静态架构图或挑几个点描述，要让读者能在大脑中模拟一次完整的执行过程。图之后必须配文字逐步讲解：每个阶段做了什么、为什么在这个时机做、边界情况走哪条路径、关键变量的状态变化。图是骨架，文字是血肉——只放图不解释等于没写。

## 面试要点
2-3 个深度追问，每个问题要呈现正反两方面考量：
- "为什么选 X 而不是更简单的 Y？"
- "如果去掉某个约束，方案会怎么变？"
- "这个设计的代价在哪？"
- 问题包含参考答案方向
```

#### 问题驱动写作 — Socratic 六问适配

将 Socratic 教学六问适配为文档写作的自检清单。每写完一节，检查是否覆盖了这些问题：

| 类型 | 问题（自检用） | 文档中的体现 |
|------|---------------|------------|
| **澄清** | 术语是否定义清晰？读者会不会产生歧义？ | 首段明确核心概念 |
| **质疑假设** | 每个方案隐藏了什么假设？（如"假设网络可靠""假设用户可信"） | 在权衡分析中说明 |
| **寻找证据** | 凭什么说这个方案"好"？有代码结构或文档依据吗？ | 引用关键文件名、目录位置、设计文档 |
| **换位思考** | 如果站在另一个项目的约束下（如 12k LOC 单文件 vs 200+ 模块化文件），方案会怎么变？ | 在面试要点中讨论 |
| **推导后果** | 这个选择后续带来了什么连锁影响？（如"选了 middleware 链 → 每个新功能都是中间件 → 代码量激增"） | 在权衡中讨论长期影响 |
| **元认知** | 理解这个模块为什么重要？它和项目的整体定位如何关联？ | 面试要点中的追问方向 |

#### 写作自检清单

每写完一篇对比文档，逐项检查：

- [ ] 开篇有 1-2 个激发好奇心的问题，不是平铺直叙
- [ ] 每个项目的方案都回答了"为什么是这种设计"而非只描述"它是什么"
- [ ] 有对比表格或 mermaid 图展示差异
- [ ] 核心机制有 step-by-step 流程解析（用 sequenceDiagram 或 stateDiagram）
- [ ] 明确标注每个方案牺牲了什么
- [ ] 面试要点包含正反两方面的深度追问
- [ ] 全文中没有比喻（"血统""骨架""心脏"这类词）
- [ ] 全文中没有成段代码（函数名可以直接出现，不用反引号标注也可）
- [ ] 全篇用中文，只在必要时出现英文术语

---

## 文件布局

```
AGENTS/
├── AGENTS.md              # 本文件
├── deer-flow/             # 原始项目
├── hermes-agent/          # 原始项目
├── openclaw-python/       # 原始项目（Python 版）
├── openclaw-2026.7.1/     # 原始项目（TS 版）
├── claudecode/            # 原始项目
├── token-optimizer/       # token 分析工具
├── docs/
│   ├── overview.md              # 四个项目的全景介绍
│   ├── comparisons/             # 按模块的横向对比文档
│   │   ├── 01-llm.md
│   │   ├── 02-agent-loop.md
│   │   ├── 03-tools.md
│   │   ├── 04-mcp.md
│   │   ├── 05-skills.md
│   │   ├── 06-memory.md
│   │   ├── 07-subagents.md
│   │   ├── 08-plugins.md
│   │   ├── 09-channels.md
│   │   ├── 10-security.md
│   │   ├── 11-config.md
│   │   └── 12-observability.md
│   ├── deer-flow/               # deer-flow 各模块实现文档
│   ├── hermes-agent/            # hermes-agent 各模块实现文档
│   ├── openclaw/                # openclaw 各模块实现文档
│   ├── claudecode/              # claudecode 各模块实现文档
│   └── summary.md               # 最终总结：各项目的适用场景与设计哲学
```

---

## 阅读源码时的注意事项

- 所有项目代码都在本地，路径如上
- deer-flow 有中文文档，但代码注释多语言混杂
- hermes-agent 的 AGENTS.md 非常长（~1700 行），要读但不要被带偏——任务是**对比**，不是复述
- openclaw-ts 的 `.agents/skills/` 里是 agent 操作技能（如 autoreview），不是用户技能；用户技能在 `skills/` 下
- openclaw-python 和 openclaw-2026.7.1 是同一项目的两种实现（Python + TS），对比时可作为一个参照组
- deer-flow 的 `backend/packages/harness/` 是核心框架，`backend/app/` 是应用层
- hermes-agent 的单文件 `run_agent.py`（~12k LOC）是核心，大但值得精读关键段落
- claudecode 的 `core/query_loop.py` 是 CC 状态机核心，与其他项目对比价值很高
- openclaw-ts 的 `src/agents/` 和 `extensions/` 是核心
